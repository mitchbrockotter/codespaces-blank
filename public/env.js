// Runtime environment configuration for the frontend.
// Set `window.API_BASE` to the base URL of your backend (no trailing slash).
// Example: window.API_BASE = 'https://your-codespace-or-ngrok-url';
// Leave as empty string to use relative paths (same host).
window.API_BASE = 'https://codespaces-blank-production-8e1d.up.railway.app';

// Optional: set to your Next.js app base URL to redirect users to the new UI.
// Example: window.APP_BASE = 'https://your-vercel-app.vercel.app';
// Leave empty to keep redirects on the legacy site.
window.APP_BASE = 'https://pkba.nl';
